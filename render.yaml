databases:
  - name: vidyaguru_db
    databaseName: vidyaguru
    user: vidyaguru
    region: oregon

services:
  # Backend API
  - type: web
    id: backend
    name: vidyaguru-backend
    runtime: python
    pythonVersion: 3.11
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 8000
    envVars:
      # Database
      - key: DATABASE_URL
        fromDatabase:
          name: vidyaguru_db
          property: connectionString
      
      # Secrets
      - key: SECRET_KEY
        scope: run
        value: ${{ secrets.SECRET_KEY }}
      
      - key: GEMINI_API_KEY
        scope: run
        value: ${{ secrets.GEMINI_API_KEY }}
      
      # Environment
      - key: ENVIRONMENT
        value: production
      
      - key: CORS_ORIGINS
        value: '["https://your-vercel-domain.vercel.app"]'
      
      - key: LOG_LEVEL
        value: INFO
      
      # Optional: Redis (can be commented out if using in-memory cache)
      - key: REDIS_URL
        value: redis://localhost:6379/0
    
    region: oregon
    repo:
      branch: main
